path: ./YOLO_detected  # Основной путь
train: train/images  # Относительный путь от 'path'
val: val/images      # Аналогично
test: test/images    # Опционально

# Автоматическое кеширование (добавьте эти строки)
cache: disk          # или 'ram' если RAM > 50GB
auto_download: False # Предотвращает неожиданные скачивания

# Классы
names: 
  0: aircraft
nc: 1               # Количество классов